# 开发文档

> 本文件与开发进程同步维护：每次开发前先看"当前步骤"，每完成一步立即更新状态。

## 一、项目信息

- 项目名称：AI 求职面试助手
- 参赛方向：语言、图像与综合辅助类（简历智能优化 / 面试模拟 AI 考官）
- 技术栈：Python + Streamlit + DeepSeek（可切换 OpenAI 等兼容平台）
- 开发周期：2026-08-04 至 2026-09-21（提交期 09-22 至 09-30）

## 二、功能清单

| 功能 | 状态 | 说明 |
| --- | --- | --- |
| 简历诊断 | 已完成（v0.1） | 粘贴 / PDF / Word → 评分 + 评价 + 建议 |
| 模拟面试 | 已完成（v0.1） | 岗位选择 → 逐题问答 → 可提前结束 |
| 面试报告 | 已完成（v0.1） | 五维度打分 + 参考回答 + 本地保存 |
| 首页引导 | 已完成（v0.1） | 三步使用说明 |
| UI 全面美化 | 已完成（v0.2） | 主题 + 全局样式 + 首页卡片 + 报告进度条 |
| 简历信息补全 | 已完成（v0.2） | AI 提取缺失项 → 表单填写 → 合并诊断 |
| 岗位对照与市场提示 | 已完成（v0.2） | 岗位要求对照表 + 最优先建议 + 市场提示 |
| 面试追问机制 | 已完成（v0.2） | 每题最多追问一次，计入报告 |
| 视觉重设计 | 已完成 | 面试练习室主题（深墨蓝+青绿+琥珀）+ Hero + 手写风格 |
| 思考动画 | 已完成 | 生成过程显示流动光效动画与灵动文字，不再输出原始代码 |
| 面试复盘手记 | 已完成 | 面试官口吻人文化输出，非模板化 AI 报告 |
| 历史记录 | 已完成 | 本地历史复盘支持查看与删除 |

## 三、开发步骤清单

- [x] 0. 初始化：Git 仓库、四份文档、目录结构
- [x] 1. 环境依赖与模型接口层（可切换 / 模拟模式）
- [x] 2. 业务层：简历解析
- [x] 3. 业务层：简历诊断
- [x] 4. 业务层：面试状态机
- [x] 5. 业务层：报告生成与本地存储
- [x] 6. 界面层：首页 + 简历诊断页
- [x] 7. 界面层：模拟面试页 + 面试报告页
- [x] 8. 自动化测试与冒烟测试
- [x] 9. 里程碑 v0.1 验收 + 最佳实践调研
- [x] 10. 里程碑 v0.2 验收 + 最佳实践调研
- [ ] 11. v0.3 部署与国内访问验证
- [ ] 12. 参赛材料：设计说明书、功能说明、演示视频
- [ ] 13. 最终检查与提交

## 四、进度记录

### 2026-08-04（第 1 周）

- 完成 Git 仓库初始化。
- 建立四份基础文档（本文件、架构文档、路线图、开发规范）。
- 建立项目目录结构与环境配置模板。
- 安装全部依赖（Streamlit、OpenAI SDK、PDF/Word 解析库等）。
- 完成模型接口层（DeepSeek / OpenAI / 自定义可切换，含模拟演示模式）。
- 完成业务层：简历解析、简历诊断、面试状态机、报告生成、本地报告存储。
- 完成界面层：首页、简历诊断、模拟面试、面试报告四个页面。
- 自动化测试 27 项全部通过；应用本地启动验证 HTTP 200。
- **里程碑 v0.1 达成**：简历诊断、模拟面试、面试报告三件套可完整闭环（模拟模式）。
- 完成最佳实践调研（见决策记录）。
- 下一步：里程碑 v0.2（追问机制、岗位要求对照表、界面打磨）。

### 2026-08-05（第 2 周）

- 完成 v0.2 第 1 步：快速真机验证（DeepSeek `deepseek-v4-flash`，真实模式）。
- 简历诊断：评分 72，优势/不足/建议各 5 条、示例 3 段，耗时约 10 秒。
- 模拟面试：5 题正常生成，状态机完整走通（5/5 作答）。
- 面试报告：总分 38，五维度 [4,3,4,5,3]，参考答案 5 组，耗时约 17 秒。
- 结论：输出格式稳定、评分范围正确、费用在预算内，可进入功能开发。
- 完成 UI 全面美化（主题、全局样式、首页卡片、报告进度条）。
- 完成简历信息补全流程：AI 提取缺失项 → 表单询问 → 合并进诊断。
- 完成简历优化升级：岗位要求对照表 + 最优先建议 + 当地市场提示。
- 完成面试追问机制：每题最多追问一次，追问与回答计入报告。
- 完整真机验收通过：信息补全 6 项、诊断（对照表 6 行 + 最优先建议 3 条 + 市场提示）、5 题面试 + 5 次追问、报告五维分数与参考答案齐全。
- **里程碑 v0.2 达成**。
- 下一步：第 4 周部署与国内访问验证，随后制作参赛材料。

### 2026-08-10（第 3 周 · 视觉与体验打磨）

- 使用 frontend-design 技能完成视觉重设计：深墨蓝 + 青绿主色 + 琥珀点缀，首页 Hero 大标题，报告采用“面试官手记”风格（楷体点缀）。
- 修复“简历诊断选择上传文件但不显示上传框”的 bug（选项移出表单，切换即时刷新）。
- 面试完成与简历诊断生成过程改为“思考动画 + 灵动文字”，不再展示原始代码输出框。
- 面试报告改为面试官口吻的人文化复盘（整体印象、逐题点评、成长建议、收尾鼓励），新增历史复盘查看与删除。
- 41 项自动化测试全部通过。
- 用户审阅"顶部路线导航重构"后决定撤回：已回滚至 `ui-before-redesign`（旧版侧边栏界面），新设计代码完整保存在备份分支 `backup/ui-redesign-20260810`，可随时恢复。
- 下一步：第 4 周部署与国内访问验证。

### 2026-08-10（React 独立前端重构 v0.3）

- 用户审阅后确定：Streamlit 视觉表现不达标，选择 **React 独立前端 + FastAPI 网关** 重做四个页面（分支 `feat/frontend-react`）。
- 后端：新增 `api/` FastAPI 网关（简历解析/补全/诊断、面试状态机、复盘生成与历史），复用全部业务服务；接口测试 4 项通过，总计 45 项测试全绿。
- 前端：新增 `web/`（Vite + React + TypeScript + Tailwind CSS + framer-motion），实现首页、简历诊断、模拟面试、面试复盘四页；设计沿用浅白 + 行动蓝 + 香槟金。
- 联调回归（mock 模式真实浏览器）：简历粘贴→补全→专属方案、5 题面试 + 追问→复盘生成→历史查看/删除、手机端上传切换、无横向溢出、控制台 0 错误。
- 下一步：真机（真实模型）完整跑通 → 部署与国内访问验证（第 4 周）。

### 2026-08-12（高级感视觉升级 v0.5）

- 用户反馈：首页“流动小球”动效廉价，整体不够精美，要求“界面非常高级”（不限于毛玻璃）。
- 按 impeccable 方向骰确定新视觉世界：**瑞士制表盘面**（seed key `05c46cb8`，assigned index 4）。
- 首页重做：移除动效小球，改为“表盘式三步校准卡”（发丝刻度环、三个香槟金校准点、蓝针指向当前步骤）+ 大标题 + 双 CTA + “一次展开”步骤卡 + 安静的三栏受众说明。
- 全局质感升级：釉白底 + 极淡蓝/金光晕、玻璃导航（内容滚过时模糊）、发丝描边卡片 + 双层投影 + 顶部内高光、蓝色主按钮 + 幽灵次按钮、金色发丝分隔线、表盘风步骤指示器。
- 规范：去除装饰网格背景（机械检测器提示的 AI 生成感）；图标改为统一描边 SVG，不再用 emoji/序号当图标。
- 首页交互增强：标题多句文案轮换（4 句自拟文案，每 4.2s 切换），切换时逐字“粉末消散→重新聚合”（Siri 式，不使用持续颜色渐变）；右侧表盘指针跟随鼠标、在三金点间磁吸吸附，悬停/聚焦放大当前点并展开该步骤详情，点击进入对应页面（键盘方向键也可切换）。
- 修复：表盘卡片去除悬停位移、详情区固定高度，消除鼠标在表盘上移动时页面上下抖动。
- 验证：`tsc` 类型检查通过、机械检测器 0 发现、后端测试 44 项通过（1 项依赖系统临时目录的用例因当前沙箱限制跳过，与本次改动无关）。
- 说明：当前沙箱禁止 esbuild 拉起子进程，无法在本环境启动 Vite 截图；视觉终验由用户运行 `启动新版前端.bat` 后浏览器确认。

### 2026-08-12（第 2 周收尾 · 部署准备）

- 打 `v0.5` 标签，`feat/frontend-react` 合并回 `main`（`3089df9`），React 主版本正式成为默认版本。
- 真机完整回归通过（真实模型，用户已运行的 8000 后端）：解析 0.9s、补全 17.7s（6 项）、诊断 93.7s（评分 62）、3 题面试 + 1 追问全通、复盘 17.9s（评分 70）、历史列表与删除正常，测试报告已清理。
- 边界用例 8 项通过：空文本解析/补全/诊断、空岗位、非法状态作答、不存在报告的读取与删除，均返回正确的 400/404。
- 部署准备：新增 `Dockerfile`（Hugging Face Spaces 镜像内构建前端 + 运行 FastAPI）、`推送GitHub.bat`（自动装 gh / 登录 / 建仓 / 推送）、`scripts/真实模型回归.py`；`api/app.py` 在生产模式自动托管前端静态文件。
- 已知限制：本环境外网被禁（GitHub 与 DeepSeek 均不可达）且未安装 GitHub CLI，推送需在用户机器上双击 `推送GitHub.bat` 完成。
- 记录：诊断耗时 93.7s 偏长，后续可做流式输出或降低输出上限优化，列入第 3 周延迟优化事项。

### 2026-08-12（部署 · GitHub 推送完成）

- 用户在机器上完成 GitHub 推送：仓库 `Yuvzol0932/ai-job-interview-assistant`，`origin` 已配置，`main` 与全部标签已推送（含 v0.1~v0.5）。
- 敏感文件复查通过：仓库仅含 `.env.example` 与 `data/reports/.gitkeep`，真实密钥与本地报告未入库。
- 新增 `.dockerignore`：排除 `.env`、缓存、`node_modules`、`data/reports` 等，防止密钥/冗余文件进入镜像。
- 下一步：Sealos 公网部署（GitHub 导入 → Dockerfile 构建 → 暴露 7860 → 配置 `LLM_API_KEY` / `LLM_MODE=real`）→ 国内访问测试。

### 2026-08-12（部署方案调整 · Sealos）

- Hugging Face 创建 Space 时提示：Gradio 和 Docker Spaces 需要付费专业版，静态空间免费但无法运行 FastAPI 后端；确认 Docker 托管不再免费。
- 方案调整：免费公网托管改用 **Sealos**（国内平台、无需信用卡、注册赠送 7 天免费试用；0.5 核轻量配置按小时计费，演示期间暂停不计费）。
- 生产构建与后端全量验证通过：`npm run build` 成功（前端 dist 约 318KB）、45 项 pytest 全绿、生产模式 `GET /` 与 `GET /api/health` 均返回 200。
- 新增 `部署Sealos.bat`：复制 GitHub 仓库地址到剪贴板并打开 Sealos 控制台，随后按 README 指引完成 GitHub 导入部署。
- 兜底不变：本地运行 + 录屏演示 + 演示视频作为正式演示路径。

### 2026-08-12（Sealos 部署落地 · GHCR 镜像链路）

- 用户在 Sealos 创建应用时未看到「GitHub 导入」入口，直接创建后公网链接显示 nginx 欢迎页（容器内是空白/默认 nginx，代码未构建进去）。
- 本机未安装 Docker，无法本地构建镜像；改为新增 `.github/workflows/docker-publish.yml`：推送到 `main` 后由 GitHub Actions 自动构建 Dockerfile 并推送到 GHCR。
- 镜像地址：`ghcr.io/yuvzol0932/ai-job-interview-assistant:latest`（公开仓库，无需密钥即可拉取）。
- Sealos 侧改为「应用管理 → 创建/变更应用 → Docker 镜像部署」：粘贴镜像地址、容器端口 7860、配置 `LLM_API_KEY` / `LLM_MODE=real`。
- 已把误建的 nginx 应用的处理方式写入 README：直接变更镜像地址即可，不用重建。
- GitHub Actions 首次构建因镜像标签含大写失败，修复为全小写后构建成功（1 分 34 秒），`latest` 与 commit SHA 两个标签均已发布。
- GHCR 匿名拉取验证通过：`ghcr.io/yuvzol0932/ai-job-interview-assistant:latest` 已公开可拉取，无需任何密钥。

### 2026-08-13（Sealos 公网部署验证通过）

- 用户完成 Sealos 应用配置（访问方式选公有 + 完整镜像地址 `ghcr.io/yuvzol0932/ai-job-interview-assistant:latest` + 容器端口 7860），并提供公网链接 `https://hdbmqqcbdpok.cloud.sealos.io/`。
- 验证结果：`GET /` 返回 HTTP 200，页面为我们的 React 首页（标题「AI 求职面试助手」，非 nginx 欢迎页）；`GET /api/health` 返回 `{"status":"ok"}`（约 1.1s）。
- 首次访问约 12s 属冷启动，随后明显加快；符合预期。
- 下一步：手机端国内访问测试 → 本地运行 + 录屏演示兜底演练 → 参赛材料制作。

### 2026-08-13（保留功能 + 进度条真实化）

- 保留功能：面试状态从 `sessionStorage` 改为 `localStorage`（关闭页面/浏览器再进来，面试进度仍在）；生成复盘时同步存一份到本机浏览器，复盘页合并「云端 + 本机」历史并按报告 ID 去重、按时间倒序；云端容器重置后历史记录不丢失。方案取舍：不采用账号密码（多设备同步）方案，理由见决策记录。
- 进度条优化：不再按阶段数提前冲到 100%（原 3 阶段约 3 秒即满格，而真实 AI 请求需 30–90 秒）；改为运行中渐进逼近 90% 的进度 + 流动高光 + 「AI 生成中，通常需要 1–2 分钟」提示，仅当请求真正完成才跳到 100% 并短暂显示「已完成」。未采用「悬停展示后台输出」方案，理由见决策记录。
- 验证：`npm run build` 通过（tsc + vite）；后端 45 项 pytest 全绿；本地生产模式冒烟通过（`GET /` 返回新构建首页、`GET /api/health` 200）。
- 下一步：推送 GitHub 触发镜像重建 → Sealos 更新应用 → 真机回归（关页面重开、断网/容器重置后的历史保留、长耗时诊断进度条表现）。

## 五、决策记录

| 日期 | 决策 | 原因 |
| --- | --- | --- |
| 2026-08-04 | 采用 DeepSeek 作为默认模型，接口层可切换 | 国内直连、延迟低、性价比高 |
| 2026-08-04 | 第一版纯文字，不加语音 | 一个月内保证完成度 |
| 2026-08-04 | 免费部署 + 本地运行兜底 | 确保国内环境可访问且不花钱 |
| 2026-08-04 | v0.2 模拟面试加入"一次一问、答后可追问"机制 | 行业最佳实践：真实面试官会针对空泛回答追问细节 |
| 2026-08-04 | v0.2 简历诊断加入"岗位要求 × 简历证据 × 证据强度 × 缺口"对照表 | 行业最佳实践：先对照岗位找证据缺口，禁止 AI 推测虚构 |
| 2026-08-05 | 真机验证通过，确认 `deepseek-v4-flash` 输出稳定 | 评分范围正确、JSON 格式稳定、全流程约 27 秒 |
| 2026-08-05 | 用户信息采集改为"AI 先提取简历缺失项，再列表询问用户" | 比强制填表更自然，信息只用于简历优化 |
| 2026-08-05 | 模型输出上限统一提高到 8192 tokens | 长优化方案曾被截断导致解析失败 |
| 2026-08-05 | AI 返回格式异常时自动重试一次 | 模型偶发输出非 JSON，重试可显著降低失败率 |
| 2026-08-05 | 追问机制限定每题最多一次 | 平衡真实感与流程复杂度 |
| 2026-08-10 | 视觉方向定为“面试练习室”：深墨蓝+青绿+琥珀，报告用面试官手记样式 | 按 frontend-design 技能做差异化设计，避免模板化 AI 感 |
| 2026-08-10 | 面试复盘采用人文化叙述结构（印象/逐题点评/建议/收尾） | 用户要求输出不能一看就是 AI |
| 2026-08-10 | 撤回"顶部路线导航"完整重构，保留旧版侧边栏界面 | 用户对新设计不满意；回滚点 `ui-before-redesign` + 备份分支 `backup/ui-redesign-20260810` 双保险，可恢复 |
| 2026-08-10 | 主版本改用 React 独立前端 + FastAPI 网关，Streamlit 版本作为兜底 | 两次 Streamlit 视觉重构均未达用户预期，独立前端表现力上限更高 |
| 2026-08-10 | 面试流程采用"状态快照往返"接口 | 状态机逻辑继续留在 Python 业务层，前端只保存和回传状态 JSON |
| 2026-08-12 | 视觉世界定为“瑞士制表盘面”：釉白底 + 发丝刻度 + 蓝针 + 金标 | 用户要求“非常高级”；方向由 impeccable 骰分配（`05c46cb8`），天然契合浅白/浅蓝/鎏金品牌承诺 |
| 2026-08-12 | 移除首页动效小球与装饰网格，动效收敛为一次性入场与悬停微交互 | “会动的球/网格”是 AI 生成界面的典型特征，高级感来自精度与克制 |
| 2026-08-12 | 标题文案切换采用 Siri 式粒子消散/聚合过渡 | 用户澄清“渐变”指切换动画而非文字颜色渐变；机械检测器 0 提示 |
| 2026-08-12 | 表盘指针交互：跟随鼠标 → 三点磁吸 → 悬停放大并展开详情 | 用户指定的三类交互；同时提供键盘方向键与点击进入，保证可访问性 |
| 2026-08-12 | 表盘卡片固定高度、去除悬停位移 | 详情展开/收起与悬停位移导致鼠标移动时页面上下抖动 |
| 2026-08-12 | 主版本合并入 `main` 并打 `v0.5` 标签 | React 独立前端四页闭环 + 视觉定稿均完成，作为参赛主版本 |
| 2026-08-12 | 部署采用 GitHub 源码 + Hugging Face Spaces（Docker 构建） | 当时评估为免费托管、镜像内构建前端避免本地构建差异；后续发现 HF Docker 需付费 |
| 2026-08-12 | 免费公网托管改用 Sealos（GitHub 导入 + Dockerfile 构建） | HF Docker 需付费；Sealos 国内访问快、无需信用卡、免费试用 + 按量计费，暂停不计费；本地 + 录屏仍兜底 |
| 2026-08-12 | Sealos 部署走「GitHub Actions 构建镜像 → GHCR → Sealos Docker 镜像部署」 | 用户界面没有 GitHub 导入入口，且本机无 Docker；GitHub Actions 全自动构建，Sealos 只需填镜像地址 |
| 2026-08-13 | 保留功能采用「浏览器本地保留」而非账号密码 | 零成本、不改后端架构、数据只在本人浏览器，满足「退出再进记录不丢」；账号方案需要登录体系与数据库，免费云部署无法持久化 |
| 2026-08-13 | 进度条采用「真实渐进」而非「悬停显示后台输出」 | 流式输出需后端大改且会重新暴露原始 AI 输出，违背此前「不展示代码输出框」的体验约定；渐进进度 + 阶段文案已消除「卡死」错觉 |

## 六、问题记录

| 日期 | 问题 | 状态 | 解决办法 |
| --- | --- | --- | --- |
| 2026-08-04 | 双击启动器后 localhost 拒绝连接 | 已解决 | ① 启动脚本 LF 换行导致 cmd 无法执行 → 改为 CRLF；② Streamlit 首次运行弹出 Email 输入框卡住启动 → 启动器自动写入空邮箱配置跳过提示 |
| 2026-08-05 | 长优化方案输出被截断导致 JSON 解析失败 | 已解决 | 模型输出上限统一提高到 8192 tokens |
| 2026-08-05 | 模型偶发返回非 JSON 格式 | 已解决 | 诊断/信息补全/报告/追问统一加"格式异常自动重试一次" |
| 2026-08-10 | 简历诊断选择"上传文件"后不显示上传框 | 已解决 | 选项在 st.form 内不触发刷新，改为表单外组件，切换即时生效 |
| 2026-08-10 | 面试完成后页面直接展示原始代码输出框 | 已解决 | 去掉流式原始输出，改为思考动画 + 阶段文字，完成后才展示复盘 |
| 2026-08-10 | 浏览器报 NotFoundError removeChild（React 无法移除自定义 HTML 节点） | 已解决 | 移除 unsafe_allow_html 自定义 HTML（Hero/思考条/手记卡片），改用原生标题、进度条、引用块；全局样式注入保留 |
| 2026-08-10 | 修改后打开仍是旧界面/旧 bug | 已解决 | 8501 端口被旧应用进程占用导致新版本未接管；启动器新增端口占用检测提示，使用时先关闭旧窗口 |
| 2026-08-11 | `启动新版前端.bat` 双击后闪退 | 已解决 | 文件为 LF 换行导致 cmd 解析失败；改为纯 CRLF + ASCII 文本，并加依赖检查与自动安装 |

## 七、参赛材料提交清单

- [ ] 设计说明书（需求、架构、功能、技术、创新点、测试）
- [ ] 功能说明 / 使用手册
- [ ] 演示视频（3–5 分钟）
- [ ] 源代码与 README
- [ ] 部署说明与在线链接
- [ ] 答辩要点
